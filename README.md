In order to run whole project use:

```sh
npm run dev
```

In case of need to run backend separately from frontend use

To run backend

```sh
cd apps/backend/ && npm run dev
```

To run frontend

```sh
cd apps/frontend/ && npm run dev
```
